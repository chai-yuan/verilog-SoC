{
  "schema_version": "1.0",
  "metadata": {
    "name": "virtual_clock_reset",
    "version": "1.0.0",
    "author": "SoC project",
    "description": "Virtual clock and reset module for simulation. Passes the external clock and reset through unchanged."
  },
  "parameters": {},
  "interfaces": {
    "CLOCK_RESET": {
      "type": "clock_reset",
      "mode": "slave",
      "description": "External simulation clock and reset inputs."
    },
    "CLOCK_RESET_OUT": {
      "type": "clock_reset",
      "mode": "master",
      "description": "Buffered clock and reset outputs feeding the DUT."
    }
  },
  "ports": {
    "clock": { "direction": "input", "width": "1", "interface": "CLOCK_RESET", "role": "clock" },
    "reset": { "direction": "input", "width": "1", "interface": "CLOCK_RESET", "role": "reset" },

    "clock_out": { "direction": "output", "width": "1", "interface": "CLOCK_RESET_OUT", "role": "clock" },
    "reset_out": { "direction": "output", "width": "1", "interface": "CLOCK_RESET_OUT", "role": "reset" }
  },
  "files": {
    "hdl": [
      "sim/clock-reset/virtual_clock_reset.v"
    ]
  }
}
